继续修复，修复思路：
1. findNode 函数增加一个参数 指示是否只返回一个值，返回值的改成 CurvePoint列表，，如果输入 ‘是’  则CurvePoint列表只返回第一个命中的；如果输入否，则返回所有命中的点，如果同意曲线有多个点，则去重，每条曲线只返回一个点。 
2. 保留MergeCurves2 函数，参考MergeCurves2  逻辑新建 MergeCurves3 函数 返回 Area列表（所有闭合的和不闭合的曲线）。MergeCurves3 里 每条线都遍历所有的点找到所有可能的交叉线。采用深度优先或广度优先的方式都可以，找到所有的环(闭合的环 IsClosed 为true,不闭合的为否)。 
3. 测试用例中，只计算闭合的环的面积。 
4. 两条曲线有多个交叉点的时候，只选一个。